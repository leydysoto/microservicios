server:
  port:8080
spring:
  cloud:
    config:
      server:
        git:
          default-label: config-server
          uri: https://github.com/leydysoto/microservicios
          search-paths: config-data
  aplication:
    name: config-service


